name: celmouse

packages:
  - mobile
  - desktop
  - packages/**
  - plugins/**

scripts:
  clean: 
    run: melos exec --ignore="*example*" -- melos clean && melos bootstrap

  protocol:
    run: melos exec --scope="*protocol*" -- dart run build_runner build --delete-conflicting-outputs