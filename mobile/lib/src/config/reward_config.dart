class RewardConfig {
  static const int videosForFreeDay = 5;
  static const Duration rewardForEachVideo = Duration(hours: 1);
  static const Duration rewardForTwoVideos = Duration(hours: 2, minutes: 30);
  static const Duration rewardForFiveVideos = Duration(days: 1);
}
